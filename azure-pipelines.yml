parameters:
- name: runTimeParam
  displayName: Test Param Name
  default: Hello
  type: string

variables:
  - group: myVarGroup
  - name: vvv1
    value: vvv1

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
  - script: |
      echo "stage variable: $(vvv1)"
      echo "pipeline param: ${{ parameters.runTimeParam }}"
      echo "variable from project variable group: $(v1)"
      echo "secret from project variable group: $(secret1)"
      echo "pipeline variable: $(MyPipelineVariable)"
      echo "pipeline secret: $(MyPipelineSecret)"
      exit 1
  - task: CmdLine@2
    condition: always()
    inputs:
      script: |
        echo "Runing task with always()"
  - task: CmdLine@2
    condition: failed()
    inputs:
      script: |
        echo "Runing task with failed()"